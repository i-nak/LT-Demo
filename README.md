# LT-Demo

LT（ライトニングトーク）デモ用の iOS アプリ。

新規プロジェクトとして作り直したスターター状態です（`ContentView.swift` の Hello, world から開始）。

## 構成

- `LTDemo.xcodeproj` — Xcode プロジェクト
- `LTDemo/ContentView.swift` — アプリのエントリポイント（`@main LTDemoApp`）と画面

## 動かし方

`LTDemo.xcodeproj` を Xcode で開いて Run。
